const Navbar = () => {
    return (
        <nav className="w-full bg-gray-800 text-white py-4 sticky top-0 z-50">
            <div className="container mx-auto px-4">
                Navbar
            </div>
        </nav>
    );
};

export default Navbar;